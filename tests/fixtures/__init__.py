"""Test fixtures for agent loop demo scenarios.

Provides reusable sample data for validating the agent loop end-to-end:
    - Wiki test spec entries (YAML frontmatter + markdown body)
    - Natural-language input phrases for each demo scenario
    - Expected intermediate outputs at each stage of the pipeline
"""
